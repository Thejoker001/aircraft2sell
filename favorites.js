/* ============================================================
   favorites.js — Aircraft2Sell
   Les favoris sont gérés par A2SFavs dans nav.js.
   Ce fichier existe pour éviter l'erreur 404.
   ============================================================ */

// A2SFavs est défini dans nav.js (déjà chargé avant ce fichier)
// Rien à faire ici — dashboard.html utilise window.A2SFavs directement
